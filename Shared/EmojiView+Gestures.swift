//
//  EmojiView+Gestures.swift
//  EmojiArt
//
//  Created by Joshua Olson on 11/17/20.
//

import SwiftUI

extension EmojiView {
//    var emojiOffset: CGSize {
//        gestureEmojiPanOffset * zoomScale
//    }
//
//    // Drag individual emoji around if in EmojiView otherwise Drag all emoji around.
//    // How can I identify which emoji is being clicked on in the dragGesture
//    var emojiPanGesture: some Gesture {
//        DragGesture()
//            .updating($gestureEmojiPanOffset) { latestDragValue, gestureEmojiPanOffset, _ in
//                gestureEmojiPanOffset = latestDragValue.translation / zoomScale
//            }
//            .onEnded { finalDragValue in
//                document.moveEmoji(emoji, by: finalDragValue.translation / zoomScale)
//            }
//    }
//
//    func position(for emoji: EmojiArt.Emoji, in size: CGSize) -> CGPoint {
//        // Adjust emoji.location to current size
//        var location = emoji.location * zoomScale
//
//        location = CGPoint(x: location.x + emojiOffset.width, y: location.y + emojiOffset.height)
//
//        // Return frame coords
//        return CGPoint(x: location.x + size.width / 2, y: location.y + size.height / 2)
//    }

}
